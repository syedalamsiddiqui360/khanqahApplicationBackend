import { SubscriptionData } from './data';
import Seal from '../../types/Seal';
import subscriptionHelpers from './helpers';
declare type Subscription = Seal<SubscriptionData, typeof subscriptionHelpers>;
export default Subscription;
export declare function injectPrototypes(input: SubscriptionData): Subscription;

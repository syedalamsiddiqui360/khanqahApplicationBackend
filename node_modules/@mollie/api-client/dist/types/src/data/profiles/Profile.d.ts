import { ProfileData } from './data';
import Seal from '../../types/Seal';
import profileHelpers from './helpers';
declare type Profile = Seal<ProfileData, typeof profileHelpers>;
export default Profile;
export declare function injectPrototypes(input: ProfileData): Profile;

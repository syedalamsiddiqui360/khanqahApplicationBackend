import { GetParameters, ListParameters } from './parameters';
import Callback from '../../../types/Callback';
import Capture, { CaptureData, injectPrototypes } from '../../../data/payments/captures/Capture';
import List from '../../../data/list/List';
import ParentedResource from '../../ParentedResource';
export default class PaymentsCapturesResource extends ParentedResource<CaptureData, Capture> {
    protected getResourceUrl(paymentId: string): string;
    protected injectPrototypes: typeof injectPrototypes;
    /**
     * Retrieve a list of Payment Captures
     *
     * @since 1.1.1
     *
     * @see https://docs.mollie.com/reference/v2/captures-api/list-captures
     */
    all: PaymentsCapturesResource['list'];
    /**
     * Retrieve a list of Payment Captures
     *
     * @since 3.0.0
     *
     * @see https://docs.mollie.com/reference/v2/captures-api/list-captures
     */
    page: PaymentsCapturesResource['list'];
    /**
     * Get a Payment Capture by ID
     *
     * @since 1.1.1
     *
     * @see https://docs.mollie.com/reference/v2/captures-api/get-Capture
     */
    get(id: string, parameters: GetParameters): Promise<Capture>;
    get(id: string, parameters: GetParameters, callback: Callback<Capture>): void;
    /**
     * Retrieve a list of Payment Captures
     *
     * @since 3.0.0
     *
     * @see https://docs.mollie.com/reference/v2/captures-api/list-captures
     */
    list(parameters: ListParameters): Promise<List<Capture>>;
    list(parameters: ListParameters, callback: Callback<List<Capture>>): void;
}
